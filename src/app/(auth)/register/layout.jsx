import React from 'react';

export const metadata = {
    title: "Register",
    description: "Create Your SkillSphere Account.",
};

const RegisterLayout = ({ children }) => {
    return children;
};

export default RegisterLayout;